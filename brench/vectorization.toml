extract = 'total_dyn_inst: (\d+)'
benchmarks = '../all-benchmarks/*.bril'
timeout = 60

[runs.baseline]
pipeline = [
    "bril2json",
    "brili-vc -p {args}",
]

[runs.vector]
pipeline = [
    "bril2json",
    "python3 ../vectorization.py --op=True",
    "brili-vc -p {args}",
]